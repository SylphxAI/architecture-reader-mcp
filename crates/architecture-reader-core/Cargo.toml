[package]
name = "architecture-reader-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core architecture evidence graph contracts for Architecture Reader MCP."

[lib]
path = "src/lib.rs"

[dependencies]
